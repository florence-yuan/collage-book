<header>
    <nav>
        <a href={import.meta.env.BASE_URL}>The First</a>
        <a href={import.meta.env.BASE_URL}>About</a>
        <a href={import.meta.env.BASE_URL + '/sitemap'}>Sitemap</a>
    </nav>
</header>

<style>
    header {
        padding: 3rem calc(3rem + 3vw) 2rem 3rem;
    }

    nav {
        display: flex;
        justify-content: right;
        gap: 2rem;
    }

    nav a {
        position: relative;
        font-size: .9em;
        font-family: 'Montserrat', sans-serif;
        font-weight: 500;
        letter-spacing: 0.1rem;
        text-transform: uppercase;
        text-decoration: none;
    }

    nav a::before {
        content: '';
        position: absolute;
        bottom: -1px;
        left: 0;
        width: 100%;
        border-top: 1px solid;
        transform-origin: left;
        transform: scaleX(0);
        transition: all 0.25s ease-in-out;
    }

    nav a:hover::before {
        transform: none;
    }
</style>