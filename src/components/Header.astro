<header>
    <nav>
        <a href={import.meta.env.BASE_URL}>The First</a>
        <a href={import.meta.env.BASE_URL}>About</a>
        <a href={import.meta.env.BASE_URL}>Sitemap</a>
    </nav>
</header>

<style>
    header {
        padding: 3rem calc(3rem + 3vw) 2rem 3rem;
    }

    nav {
        display: flex;
        justify-content: right;
        gap: 1.5rem;
    }

    nav a {
        font-size: .9em;
        font-family: 'Montserrat', sans-serif;
        font-weight: 500;
        letter-spacing: 0.1rem;
        text-transform: uppercase;
        text-decoration: none;
    }
</style>